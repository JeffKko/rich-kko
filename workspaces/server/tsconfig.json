{
  "extends": "../../tsconfig",
  "compilerOptions": {
    "outDir": "./dist", // 指定編譯後的檔案存放點
    "rootDir": "./src", // 載入點的位置
    "baseUrl": ".", // 指定匯入檔案的基準路徑
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src/**/*.ts"] // 納入編譯範圍
}
